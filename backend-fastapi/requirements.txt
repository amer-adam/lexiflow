fastapi
uvicorn[standard]
python-multipart
requests
python-dotenv
json5
pycccedict
pydantic
yt-dlp
# yt-dlp plugin - generates the PO (proof-of-origin) token YouTube now requires
# for the "web" player client once real cookies are attached. Talks to the
# bgutil-pot-provider sidecar - see src/ytdlpPot.py / YTDLP_POT_PROVIDER_URL.
bgutil-ytdlp-pot-provider
# SOCKS5 proxy support for yt-dlp - see src/ytdlpProxy.py / YTDLP_PROXY_URL
# (an SSH-reverse-tunneled proxy back to a non-datacenter IP).
PySocks
ffmpeg-python
numpy
pydub
pymongo
lingua-language-detector
pypinyin
jieba
# No local model inference anywhere: Whisper transcription, transcript/title
# translation, and quiz judging/distractor generation are all API-based (see
# src/whisper/apiWhisperContainer.py, src/llm/translator.py,
# services/quiz/judge.py, services/quiz/distractors.py) - no torch,
# transformers, or sentence-transformers needed, and no GPU required anywhere.
