fastapi
uvicorn[standard]
python-multipart
requests
python-dotenv
json5
pycccedict
pydantic
yt-dlp
ffmpeg-python
numpy
pydub
pymongo
lingua-language-detector
pypinyin
openai-whisper
faster-whisper
# Pinned to a CUDA 12.4 build — driver 575.x (CUDA 12.9 max) can't run the
# CUDA 13 wheels that "torch" (unpinned) resolves to as of late 2026, which
# fails with "CUDA driver version is insufficient for CUDA runtime version".
# Bump these only alongside a host NVIDIA driver upgrade that supports the
# newer CUDA major version.
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.5.1+cu124
torchaudio==2.5.1+cu124
# Pinned to the version already validated against this driver — ctranslate2's
# bundled CUDA runtime libs are independent of torch's, leave unless retested.
ctranslate2==4.8.0
onnxruntime
jieba
transformers
sentence-transformers
